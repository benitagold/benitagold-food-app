# IranPayamak / FarazSMS API credentials — DO NOT commit real values.
IRANPAYAMAK_API_KEY=your-api-key-here
IRANPAYAMAK_LINE_NUMBER=your-sender-line-number

# Passwords for each location's submission form.
GALLERY_PASSWORD=choose-a-password-for-mr-marasi
OFFICE_PASSWORD=choose-a-password-for-mrs-ganjavi

# KV_REST_API_URL and KV_REST_API_TOKEN are provided automatically by Vercel
# once you connect a Vercel KV (Upstash Redis) database to this project —
# you do NOT need to set them manually. See README for the one-time setup.
